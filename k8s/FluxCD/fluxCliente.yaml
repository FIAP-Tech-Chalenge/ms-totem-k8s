apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: ms-cliente-api
spec:
  image: wolwer/ms-cliente-api
  interval: 1m

---

apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: ms-cliente-api-policy
spec:
  imageRepositoryRef:
    name: ms-cliente-api
  policy:
    semver:
      range: ">=v1.0.0"

---

apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageUpdateAutomation
metadata:
  name: ms-cliente-api-update
spec:
  git:
    checkout:
      ref:
        branch: main
    commit:
      author:
        email: ricardo@dtmm.com.br
        name: FluxCD
      messageTemplate: "Update image to {{ .ImageName }}:{{ .NewTag }}"
    push:
      branch: main
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: ms-cliente-api
  update:
    strategy: Setters

---

apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: ms-cliente-api
spec:
  interval: 1m
  url: https://github.com/FIAP-Tech-Chalenge/ms-cliente-api.git
  ref:
    branch: main