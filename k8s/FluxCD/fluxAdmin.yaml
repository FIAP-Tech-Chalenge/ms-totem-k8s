apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: ms-admin-api
  namespace: flux-system
spec:
  image: wolwer/ms-admin-api
  interval: 5m

---

apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: ms-admin-api-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: ms-admin-api
  policy:
    semver:
      range: 1.0.x

---

apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageUpdateAutomation
metadata:
  name: ms-admin-api-update
  namespace: flux-system
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: ms-admin-api
  git:
    checkout:
      ref:
        branch: main
    url: https://github.com/FIAP-Tech-Chalenge/ms-admin-api
    commit:
      author:
        email: ricardo@dtmm.com.br
        name: FluxCD
      messageTemplate: 'Update image to {{ .ImageName }}:{{ .NewTag }}'
    push:
      branch: flux-updates
  update:
    strategy: Setters